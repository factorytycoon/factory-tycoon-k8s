apiVersion: v1
kind: Service
metadata:
  name: sf-backend-aws
  labels:
    app: sf-backend-aws
spec:
  type: ClusterIP
  selector:
    app: sf-backend-aws
  ports:
    - port: 8081
      targetPort: 8081
